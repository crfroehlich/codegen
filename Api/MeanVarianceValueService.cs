//------------------------------------------------------------------------------
// <auto-generated>
//    This code is auto-generated. This class can be only customized modifying the corresponding partial class.
//    All other changes to this file will cause incorrect behavior and will be lost when
//    the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using AutoMapper;

using Services.Core;
using Services.Db;
using Services.Dto;
using Services.Dto.Security;
using Services.Enums;
using Services.Models;
using Services.Schema;

using Typed;
using Typed.Bindings;
using Typed.Notifications;
using Typed.Settings;

using ServiceStack;
using ServiceStack.Text;

using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Linq;
using System.Linq.Dynamic;
using System.Net;
using System.Web;

using Xtensive.Orm;
using Xtensive.Orm.Model;

using Attribute = Services.Dto.Attribute;
using ValueType = Services.Dto.ValueType;
using Version = Services.Dto.Version;

namespace Services.API
{
    public partial class MeanVarianceValueService : DocServiceBase
    {
        private IQueryable<DocEntityMeanVarianceValue> _ExecSearch(MeanVarianceValueSearch request)
        {
            request = InitSearch<MeanVarianceValue, MeanVarianceValueSearch>(request);
            IQueryable<DocEntityMeanVarianceValue> entities = null;
            Execute.Run( session => 
            {
                entities = Execute.SelectAll<DocEntityMeanVarianceValue>();
                if(!DocTools.IsNullOrEmpty(request.FullTextSearch))
                {
                    var fts = new MeanVarianceValueFullTextSearch(request);
                    entities = GetFullTextSearch<DocEntityMeanVarianceValue,MeanVarianceValueFullTextSearch>(fts, entities);
                }

                if(null != request.Ids && request.Ids.Any())
                    entities = entities.Where(en => en.Id.In(request.Ids));

                if (!DocTools.IsNullOrEmpty(request.Updated))
                {
                    entities = entities.Where(e => null != e.Updated && e.Updated.Value.Date == request.Updated.Value.Date);
                }
                if (!DocTools.IsNullOrEmpty(request.UpdatedBefore))
                {
                    entities = entities.Where(e => null != e.Updated && e.Updated <= request.UpdatedBefore);
                }
                if( !DocTools.IsNullOrEmpty( request.UpdatedAfter ) )
                {
                    entities = entities.Where(e => null != e.Updated && e.Updated >= request.UpdatedAfter);
                }
                if (!DocTools.IsNullOrEmpty(request.Created))
                {
                    entities = entities.Where(e => null!= e.Created && e.Created.Value.Date == request.Created.Value.Date);
                }
                if (!DocTools.IsNullOrEmpty(request.CreatedBefore))
                {
                    entities = entities.Where(e => null!= e.Created && e.Created <= request.CreatedBefore);
                }
                if( !DocTools.IsNullOrEmpty( request.CreatedAfter ) )
                {
                    entities = entities.Where(e => null!= e.Created && e.Created >= request.CreatedAfter);
                }

                if(!DocTools.IsNullOrEmpty(request.MeanVarianceType) && !DocTools.IsNullOrEmpty(request.MeanVarianceType.Id))
                {
                    entities = entities.Where(en => en.MeanVarianceType.Id == request.MeanVarianceType.Id );
                }
                if(true == request.MeanVarianceTypeIds?.Any())
                {
                    entities = entities.Where(en => en.MeanVarianceType.Id.In(request.MeanVarianceTypeIds));
                }
                else if(!DocTools.IsNullOrEmpty(request.MeanVarianceType) && !DocTools.IsNullOrEmpty(request.MeanVarianceType.Name))
                {
                    entities = entities.Where(en => en.MeanVarianceType.Name == request.MeanVarianceType.Name );
                }
                if(true == request.MeanVarianceTypeNames?.Any())
                {
                    entities = entities.Where(en => en.MeanVarianceType.Name.In(request.MeanVarianceTypeNames));
                }
                if(request.Order.HasValue)
                    entities = entities.Where(en => request.Order.Value == en.Order);
                if(true == request.OwnersIds?.Any())
                {
                    entities = entities.Where(en => en.Owners.Any(r => r.Id.In(request.OwnersIds)));
                }

                entities = ApplyFilters<DocEntityMeanVarianceValue,MeanVarianceValueSearch>(request, entities);

                if(request.Skip > 0)
                    entities = entities.Skip(request.Skip.Value);
                if(request.Take > 0)
                    entities = entities.Take(request.Take.Value);
                if(true == request?.OrderBy?.Any())
                    entities = entities.OrderBy(request.OrderBy);
                if(true == request?.OrderByDesc?.Any())
                    entities = entities.OrderByDescending(request.OrderByDesc);
            });
            return entities;
        }

        public List<MeanVarianceValue> Post(MeanVarianceValueSearch request) => Get(request);

        public List<MeanVarianceValue> Get(MeanVarianceValueSearch request) => GetSearchResult<MeanVarianceValue,DocEntityMeanVarianceValue,MeanVarianceValueSearch>(DocConstantModelName.MEANVARIANCEVALUE, request, _ExecSearch);

        public MeanVarianceValue Get(MeanVarianceValue request) => GetEntity<MeanVarianceValue>(DocConstantModelName.MEANVARIANCEVALUE, request, GetMeanVarianceValue);
        private MeanVarianceValue _AssignValues(MeanVarianceValue request, DocConstantPermission permission, Session session)
        {
            if(permission != DocConstantPermission.ADD && (request == null || request.Id <= 0))
                throw new HttpError(HttpStatusCode.NotFound, $"No record");

            if(permission == DocConstantPermission.ADD && !DocPermissionFactory.HasPermissionTryAdd(currentUser, "MeanVarianceValue"))
                throw new HttpError(HttpStatusCode.Forbidden, "You do not have ADD permission for this route.");

            request.VisibleFields = request.VisibleFields ?? new List<string>();

            MeanVarianceValue ret = null;
            request = _InitAssignValues<MeanVarianceValue>(request, permission, session);
            //In case init assign handles create for us, return it
            if(permission == DocConstantPermission.ADD && request.Id > 0) return request;
            
            var cacheKey = GetApiCacheKey<MeanVarianceValue>(DocConstantModelName.MEANVARIANCEVALUE, nameof(MeanVarianceValue), request);
            
            //First, assign all the variables, do database lookups and conversions
            var pMeanVariance = request.MeanVariance;
            var pMeanVarianceRange = request.MeanVarianceRange;
            DocEntityLookupTable pMeanVarianceType = GetLookup(DocConstantLookupTable.MEANVARIANCETYPE, request.MeanVarianceType?.Name, request.MeanVarianceType?.Id);
            var pOrder = request.Order;
            var pOwners = request.Owners?.ToList();

            DocEntityMeanVarianceValue entity = null;
            if(permission == DocConstantPermission.ADD)
            {
                var now = DateTime.UtcNow;
                entity = new DocEntityMeanVarianceValue(session)
                {
                    Created = now,
                    Updated = now
                };
            }
            else
            {
                entity = DocEntityMeanVarianceValue.GetMeanVarianceValue(request.Id);
                if(null == entity)
                    throw new HttpError(HttpStatusCode.NotFound, $"No record");
            }

            if (DocPermissionFactory.IsRequestedHasPermission<TypeUnits>(currentUser, request, pMeanVariance, permission, DocConstantModelName.MEANVARIANCEVALUE, nameof(request.MeanVariance)))
            {
                if(DocPermissionFactory.IsRequested(request, pMeanVariance, entity.MeanVariance, nameof(request.MeanVariance)))
                    entity.MeanVariance = pMeanVariance;
                if(DocPermissionFactory.IsRequested<TypeUnits>(request, pMeanVariance, nameof(request.MeanVariance)) && !request.VisibleFields.Matches(nameof(request.MeanVariance), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.MeanVariance));
                }
            }
            if (DocPermissionFactory.IsRequestedHasPermission<TypeUnitsRange>(currentUser, request, pMeanVarianceRange, permission, DocConstantModelName.MEANVARIANCEVALUE, nameof(request.MeanVarianceRange)))
            {
                if(DocPermissionFactory.IsRequested(request, pMeanVarianceRange, entity.MeanVarianceRange, nameof(request.MeanVarianceRange)))
                    entity.MeanVarianceRange = pMeanVarianceRange;
                if(DocPermissionFactory.IsRequested<TypeUnitsRange>(request, pMeanVarianceRange, nameof(request.MeanVarianceRange)) && !request.VisibleFields.Matches(nameof(request.MeanVarianceRange), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.MeanVarianceRange));
                }
            }
            if (DocPermissionFactory.IsRequestedHasPermission<DocEntityLookupTable>(currentUser, request, pMeanVarianceType, permission, DocConstantModelName.MEANVARIANCEVALUE, nameof(request.MeanVarianceType)))
            {
                if(DocPermissionFactory.IsRequested(request, pMeanVarianceType, entity.MeanVarianceType, nameof(request.MeanVarianceType)))
                    entity.MeanVarianceType = pMeanVarianceType;
                if(DocPermissionFactory.IsRequested<DocEntityLookupTable>(request, pMeanVarianceType, nameof(request.MeanVarianceType)) && !request.VisibleFields.Matches(nameof(request.MeanVarianceType), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.MeanVarianceType));
                }
            }
            if (DocPermissionFactory.IsRequestedHasPermission<int?>(currentUser, request, pOrder, permission, DocConstantModelName.MEANVARIANCEVALUE, nameof(request.Order)))
            {
                if(DocPermissionFactory.IsRequested(request, pOrder, entity.Order, nameof(request.Order)))
                    if(null != pOrder)
                        entity.Order = (int) pOrder;
                if(DocPermissionFactory.IsRequested<int?>(request, pOrder, nameof(request.Order)) && !request.VisibleFields.Matches(nameof(request.Order), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.Order));
                }
            }
            
            if (request.Locked) entity.Locked = request.Locked;

            entity.SaveChanges(permission);
            
            if (DocPermissionFactory.IsRequestedHasPermission<List<Reference>>(currentUser, request, pOwners, permission, DocConstantModelName.MEANVARIANCEVALUE, nameof(request.Owners)))
            {
                if (true == pOwners?.Any() )
                {
                    var requestedOwners = pOwners.Select(p => p.Id).Distinct().ToList();
                    var existsOwners = Execute.SelectAll<DocEntityMeanVariances>().Where(e => e.Id.In(requestedOwners)).Select( e => e.Id ).ToList();
                    if (existsOwners.Count != requestedOwners.Count)
                    {
                        var nonExists = requestedOwners.Where(id => existsOwners.All(eId => eId != id));
                        throw new HttpError(HttpStatusCode.NotFound, $"Cannot patch collection Owners with objects that do not exist. No matching Owners(s) could be found for Ids: {nonExists.ToDelimitedString()}.");
                    }
                    var toAdd = requestedOwners.Where(id => entity.Owners.All(e => e.Id != id)).ToList(); 
                    toAdd?.ForEach(id =>
                    {
                        var target = DocEntityMeanVariances.GetMeanVariances(id);
                        if(!DocPermissionFactory.HasPermission(entity, currentUser, DocConstantPermission.ADD, targetEntity: target, targetName: nameof(MeanVarianceValue), columnName: nameof(request.Owners)))
                            throw new HttpError(HttpStatusCode.Forbidden, "You do not have permission to add {nameof(request.Owners)} to {nameof(MeanVarianceValue)}");
                        entity.Owners.Add(target);
                    });
                    var toRemove = entity.Owners.Where(e => requestedOwners.All(id => e.Id != id)).Select(e => e.Id).ToList(); 
                    toRemove.ForEach(id =>
                    {
                        var target = DocEntityMeanVariances.GetMeanVariances(id);
                        if(!DocPermissionFactory.HasPermission(entity, currentUser, DocConstantPermission.REMOVE, targetEntity: target, targetName: nameof(MeanVarianceValue), columnName: nameof(request.Owners)))
                            throw new HttpError(HttpStatusCode.Forbidden, "You do not have permission to remove {nameof(request.Owners)} from {nameof(MeanVarianceValue)}");
                        entity.Owners.Remove(target);
                    });
                }
                else
                {
                    var toRemove = entity.Owners.Select(e => e.Id).ToList(); 
                    toRemove.ForEach(id =>
                    {
                        var target = DocEntityMeanVariances.GetMeanVariances(id);
                        if(!DocPermissionFactory.HasPermission(entity, currentUser, DocConstantPermission.REMOVE, targetEntity: target, targetName: nameof(MeanVarianceValue), columnName: nameof(request.Owners)))
                            throw new HttpError(HttpStatusCode.Forbidden, "You do not have permission to remove {nameof(request.Owners)} from {nameof(MeanVarianceValue)}");
                        entity.Owners.Remove(target);
                    });
                }
                if(DocPermissionFactory.IsRequested<List<Reference>>(request, pOwners, nameof(request.Owners)) && !request.VisibleFields.Matches(nameof(request.Owners), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.Owners));
                }
            }
            DocPermissionFactory.SetVisibleFields<MeanVarianceValue>(currentUser, nameof(MeanVarianceValue), request.VisibleFields);
            ret = entity.ToDto();

            DocCacheClient.Set(key: cacheKey, value: ret, entityId: request.Id, entityType: DocConstantModelName.MEANVARIANCEVALUE);

            return ret;
        }
        public MeanVarianceValue Post(MeanVarianceValue request)
        {
            if(request == null) throw new HttpError(HttpStatusCode.NotFound, "Request cannot be null.");

            request.VisibleFields = request.VisibleFields ?? new List<string>();

            MeanVarianceValue ret = null;

            Execute.Run(ssn =>
            {
                if(!DocPermissionFactory.HasPermissionTryAdd(currentUser, "MeanVarianceValue")) 
                    throw new HttpError(HttpStatusCode.Forbidden, "You do not have ADD permission for this route.");

                ret = _AssignValues(request, DocConstantPermission.ADD, ssn);
            });

            return ret;
        }
   
        public List<MeanVarianceValue> Post(MeanVarianceValueBatch request)
        {
            if(true != request?.Any()) throw new HttpError(HttpStatusCode.NotFound, "Request cannot be empty.");

            var ret = new List<MeanVarianceValue>();
            var errors = new List<ResponseError>();
            var errorMap = new Dictionary<string, string>();
            var i = 0;
            request.ForEach(dto =>
            {
                try
                {
                    var obj = Post(dto) as MeanVarianceValue;
                    ret.Add(obj);
                    errorMap[$"{i}"] = $"{obj.Id}";
                }
                catch (Exception ex)
                {
                    errorMap[$"{i}"] = null;
                    errors.Add(new ResponseError()
                    {
                        Message = $"{ex.Message}{Environment.NewLine}{ex.InnerException?.Message}",
                        ErrorCode = $"{i}"
                    });
                }
                i += 1;
            });
            base.Response.AddHeader("X-AutoBatch-Completed", $"{ret.Count} succeeded");
            if (errors.Any())
            {
                throw new HttpError(HttpStatusCode.BadRequest, $"{errors.Count} failed in batch")
                {
                    Response = new ErrorResponse()
                    {
                        ResponseStatus = new ResponseStatus
                        {
                            ErrorCode = nameof(HttpError),
                            Meta = errorMap,
                            Message = "Incomplete request",
                            Errors = errors
                        }
                    }
                };
            }
            return ret;
        }

        public MeanVarianceValue Post(MeanVarianceValueCopy request)
        {
            MeanVarianceValue ret = null;
            Execute.Run(ssn =>
            {
                var entity = DocEntityMeanVarianceValue.GetMeanVarianceValue(request?.Id);
                if(null == entity) throw new HttpError(HttpStatusCode.NoContent, "The COPY request did not succeed.");
                if(!DocPermissionFactory.HasPermission(entity, currentUser, DocConstantPermission.ADD))
                    throw new HttpError(HttpStatusCode.Forbidden, "You do not have ADD permission for this route.");
                
                    var pMeanVariance = entity.MeanVariance;
                    var pMeanVarianceRange = entity.MeanVarianceRange;
                    var pMeanVarianceType = entity.MeanVarianceType;
                    var pOrder = entity.Order;
                    var pOwners = entity.Owners.ToList();
                #region Custom Before copyMeanVarianceValue
                #endregion Custom Before copyMeanVarianceValue
                var copy = new DocEntityMeanVarianceValue(ssn)
                {
                    Hash = Guid.NewGuid()
                                , MeanVariance = pMeanVariance
                                , MeanVarianceRange = pMeanVarianceRange
                                , MeanVarianceType = pMeanVarianceType
                                , Order = pOrder
                };
                            foreach(var item in pOwners)
                            {
                                entity.Owners.Add(item);
                            }

                #region Custom After copyMeanVarianceValue
                #endregion Custom After copyMeanVarianceValue
                copy.SaveChanges(DocConstantPermission.ADD);
                ret = copy.ToDto();
            });
            return ret;
        }


        public List<MeanVarianceValue> Put(MeanVarianceValueBatch request)
        {
            return Patch(request);
        }

        public MeanVarianceValue Put(MeanVarianceValue request)
        {
            return Patch(request);
        }

        public List<MeanVarianceValue> Patch(MeanVarianceValueBatch request)
        {
            if(true != request?.Any()) throw new HttpError(HttpStatusCode.NotFound, "Request cannot be empty.");

            var ret = new List<MeanVarianceValue>();
            var errors = new List<ResponseError>();
            var errorMap = new Dictionary<string, string>();
            var i = 0;
            request.ForEach(dto =>
            {
                try
                {
                    var obj = Patch(dto) as MeanVarianceValue;
                    ret.Add(obj);
                    errorMap[$"{i}"] = $"true";
                }
                catch (Exception ex)
                {
                    errorMap[$"{i}"] = $"false";
                    errors.Add(new ResponseError()
                    {
                        Message = $"{ex.Message}{Environment.NewLine}{ex.InnerException?.Message}",
                        ErrorCode = $"{i}"
                    });
                }
                i += 1;
            });
            base.Response.AddHeader("X-AutoBatch-Completed", $"{ret.Count} succeeded");
            if (errors.Any())
            {
                throw new HttpError(HttpStatusCode.BadRequest, $"{errors.Count} failed in batch")
                {
                    Response = new ErrorResponse()
                    {
                        ResponseStatus = new ResponseStatus
                        {
                            ErrorCode = nameof(HttpError),
                            Meta = errorMap,
                            Message = "Incomplete request",
                            Errors = errors
                        }
                    }
                };
            }
            return ret;
        }

        public MeanVarianceValue Patch(MeanVarianceValue request)
        {
            if(true != (request?.Id > 0)) throw new HttpError(HttpStatusCode.NotFound, "Please specify a valid Id of the MeanVarianceValue to patch.");
            
            request.VisibleFields = request.VisibleFields ?? new List<string>();
            
            MeanVarianceValue ret = null;
            Execute.Run(ssn =>
            {
                ret = _AssignValues(request, DocConstantPermission.EDIT, ssn);
            });
            return ret;
        }

        public void Delete(MeanVarianceValueBatch request)
        {
            if(true != request?.Any()) throw new HttpError(HttpStatusCode.NotFound, "Request cannot be empty.");

            var errors = new List<ResponseError>();
            var errorMap = new Dictionary<string, string>();
            var i = 0;
            request.ForEach(dto =>
            {
                try
                {
                    Delete(dto);
                    errorMap[$"{i}"] = $"true";
                }
                catch (Exception ex)
                {
                    errorMap[$"{i}"] = $"false";
                    errors.Add(new ResponseError()
                    {
                        Message = $"{ex.Message}{Environment.NewLine}{ex.InnerException?.Message}",
                        ErrorCode = $"{i}"
                    });
                }
                i += 1;
            });
            base.Response.AddHeader("X-AutoBatch-Completed", $"{request.Count-errors.Count} succeeded");
            if (errors.Any())
            {
                throw new HttpError(HttpStatusCode.BadRequest, $"{errors.Count} failed in batch")
                {
                    Response = new ErrorResponse()
                    {
                        ResponseStatus = new ResponseStatus
                        {
                            ErrorCode = nameof(HttpError),
                            Meta = errorMap,
                            Message = "Incomplete request",
                            Errors = errors
                        }
                    }
                };
            }
        }

        public void Delete(MeanVarianceValue request)
        {
            Execute.Run(ssn =>
            {
                if(!(request?.Id > 0)) throw new HttpError(HttpStatusCode.NotFound, $"No Id provided for delete.");

                DocCacheClient.RemoveSearch(DocConstantModelName.MEANVARIANCEVALUE);
                DocCacheClient.RemoveById(request.Id);
                var en = DocEntityMeanVarianceValue.GetMeanVarianceValue(request?.Id);

                if(null == en) throw new HttpError(HttpStatusCode.NotFound, $"No MeanVarianceValue could be found for Id {request?.Id}.");
                if(en.IsRemoved) return;
                
                if(!DocPermissionFactory.HasPermission(en, currentUser, DocConstantPermission.DELETE))
                    throw new HttpError(HttpStatusCode.Forbidden, "You do not have DELETE permission for this route.");
                
                en.Remove();
            });
        }

        public void Delete(MeanVarianceValueSearch request)
        {
            var matches = Get(request) as List<MeanVarianceValue>;
            if(true != matches?.Any()) throw new HttpError(HttpStatusCode.NotFound, "No matches for request");

            Execute.Run(ssn =>
            {
                matches.ForEach(match =>
                {
                    Delete(match);
                });
            });
        }
        public object Get(MeanVarianceValueJunction request) =>
            Execute.Run( s => 
            {
                switch(request.Junction.ToLower().TrimAndPruneSpaces())
                {
                    case "meanvariances":
                        return GetJunctionSearchResult<MeanVarianceValue, DocEntityMeanVarianceValue, DocEntityMeanVariances, MeanVariances, MeanVariancesSearch>((int)request.Id, DocConstantModelName.MEANVARIANCES, "Owners", request, (ss) => HostContext.ResolveService<MeanVariancesService>(Request)?.Get(ss));
                    default:
                        throw new HttpError(HttpStatusCode.NotFound, $"Route for meanvariancevalue/{request.Id}/{request.Junction} was not found");
                }
            });
        public object Post(MeanVarianceValueJunction request) =>
            Execute.Run( ssn =>
            {
                switch(request.Junction.ToLower().TrimAndPruneSpaces())
                {
                    case "meanvariances":
                        return AddJunction<MeanVarianceValue, DocEntityMeanVarianceValue, DocEntityMeanVariances, MeanVariances, MeanVariancesSearch>((int)request.Id, DocConstantModelName.MEANVARIANCES, "Owners", request);
                    default:
                        throw new HttpError(HttpStatusCode.NotFound, $"Route for meanvariancevalue/{request.Id}/{request.Junction} was not found");
                }
            });

        public object Delete(MeanVarianceValueJunction request) =>
            Execute.Run( ssn =>
            {
                switch(request.Junction.ToLower().TrimAndPruneSpaces())
                {
                    case "meanvariances":
                        return RemoveJunction<MeanVarianceValue, DocEntityMeanVarianceValue, DocEntityMeanVariances, MeanVariances, MeanVariancesSearch>((int)request.Id, DocConstantModelName.MEANVARIANCES, "Owners", request);
                    default:
                        throw new HttpError(HttpStatusCode.NotFound, $"Route for meanvariancevalue/{request.Id}/{request.Junction} was not found");
                }
            });

        private MeanVarianceValue GetMeanVarianceValue(MeanVarianceValue request)
        {
            var id = request?.Id;
            MeanVarianceValue ret = null;
            var query = DocQuery.ActiveQuery ?? Execute;

            DocPermissionFactory.SetVisibleFields<MeanVarianceValue>(currentUser, "MeanVarianceValue", request.VisibleFields);

            DocEntityMeanVarianceValue entity = null;
            if(id.HasValue)
            {
                entity = DocEntityMeanVarianceValue.GetMeanVarianceValue(id.Value);
            }
            if(null == entity)
                throw new HttpError(HttpStatusCode.NotFound, $"No MeanVarianceValue found for Id {id.Value}");

            if(!DocPermissionFactory.HasPermission(entity, currentUser, DocConstantPermission.VIEW))
                throw new HttpError(HttpStatusCode.Forbidden, "You do not have VIEW permission for this route.");
            
            ret = entity?.ToDto();
            return ret;
        }
    }
}