//------------------------------------------------------------------------------
// <auto-generated>
//    This code is auto-generated. This class can be only customized modifying the corresponding partial class.
//    All other changes to this file will cause incorrect behavior and will be lost when
//    the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using AutoMapper;

using Services.Core;
using Services.Db;
using Services.Dto;
using Services.Dto.Security;
using Services.Enums;
using Services.Models;
using Services.Schema;

using Typed;
using Typed.Bindings;
using Typed.Notifications;
using Typed.Settings;

using ServiceStack;
using ServiceStack.Text;

using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Linq;
using System.Linq.Dynamic;
using System.Net;
using System.Web;

using Xtensive.Orm;
using Xtensive.Orm.Model;

using Attribute = Services.Dto.Attribute;
using ValueType = Services.Dto.ValueType;
using Version = Services.Dto.Version;

namespace Services.API
{
    public partial class MeanRangeValueService : DocServiceBase
    {
        private IQueryable<DocEntityMeanRangeValue> _ExecSearch(MeanRangeValueSearch request)
        {
            request = InitSearch<MeanRangeValue, MeanRangeValueSearch>(request);
            IQueryable<DocEntityMeanRangeValue> entities = null;
            Execute.Run( session => 
            {
                entities = Execute.SelectAll<DocEntityMeanRangeValue>();
                if(!DocTools.IsNullOrEmpty(request.FullTextSearch))
                {
                    var fts = new MeanRangeValueFullTextSearch(request);
                    entities = GetFullTextSearch<DocEntityMeanRangeValue,MeanRangeValueFullTextSearch>(fts, entities);
                }

                if(null != request.Ids && request.Ids.Any())
                    entities = entities.Where(en => en.Id.In(request.Ids));

                if (!DocTools.IsNullOrEmpty(request.Updated))
                {
                    entities = entities.Where(e => null != e.Updated && e.Updated.Value.Date == request.Updated.Value.Date);
                }
                if (!DocTools.IsNullOrEmpty(request.UpdatedBefore))
                {
                    entities = entities.Where(e => null != e.Updated && e.Updated <= request.UpdatedBefore);
                }
                if( !DocTools.IsNullOrEmpty( request.UpdatedAfter ) )
                {
                    entities = entities.Where(e => null != e.Updated && e.Updated >= request.UpdatedAfter);
                }
                if (!DocTools.IsNullOrEmpty(request.Created))
                {
                    entities = entities.Where(e => null!= e.Created && e.Created.Value.Date == request.Created.Value.Date);
                }
                if (!DocTools.IsNullOrEmpty(request.CreatedBefore))
                {
                    entities = entities.Where(e => null!= e.Created && e.Created <= request.CreatedBefore);
                }
                if( !DocTools.IsNullOrEmpty( request.CreatedAfter ) )
                {
                    entities = entities.Where(e => null!= e.Created && e.Created >= request.CreatedAfter);
                }

                if(!DocTools.IsNullOrEmpty(request.MeanVarianceType) && !DocTools.IsNullOrEmpty(request.MeanVarianceType.Id))
                {
                    entities = entities.Where(en => en.MeanVarianceType.Id == request.MeanVarianceType.Id );
                }
                if(true == request.MeanVarianceTypeIds?.Any())
                {
                    entities = entities.Where(en => en.MeanVarianceType.Id.In(request.MeanVarianceTypeIds));
                }
                else if(!DocTools.IsNullOrEmpty(request.MeanVarianceType) && !DocTools.IsNullOrEmpty(request.MeanVarianceType.Name))
                {
                    entities = entities.Where(en => en.MeanVarianceType.Name == request.MeanVarianceType.Name );
                }
                if(true == request.MeanVarianceTypeNames?.Any())
                {
                    entities = entities.Where(en => en.MeanVarianceType.Name.In(request.MeanVarianceTypeNames));
                }
                if(request.Order.HasValue)
                    entities = entities.Where(en => request.Order.Value == en.Order);
                if(true == request.OwnersIds?.Any())
                {
                    entities = entities.Where(en => en.Owners.Any(r => r.Id.In(request.OwnersIds)));
                }
                if(request.Percent.HasValue)
                    entities = entities.Where(en => request.Percent.Value == en.Percent);
                if(request.PercentLow.HasValue)
                    entities = entities.Where(en => request.PercentLow.Value == en.PercentLow);
                if(!DocTools.IsNullOrEmpty(request.Type) && !DocTools.IsNullOrEmpty(request.Type.Id))
                {
                    entities = entities.Where(en => en.Type.Id == request.Type.Id );
                }
                if(true == request.TypeIds?.Any())
                {
                    entities = entities.Where(en => en.Type.Id.In(request.TypeIds));
                }
                else if(!DocTools.IsNullOrEmpty(request.Type) && !DocTools.IsNullOrEmpty(request.Type.Name))
                {
                    entities = entities.Where(en => en.Type.Name == request.Type.Name );
                }
                if(true == request.TypeNames?.Any())
                {
                    entities = entities.Where(en => en.Type.Name.In(request.TypeNames));
                }

                entities = ApplyFilters<DocEntityMeanRangeValue,MeanRangeValueSearch>(request, entities);

                if(request.Skip > 0)
                    entities = entities.Skip(request.Skip.Value);
                if(request.Take > 0)
                    entities = entities.Take(request.Take.Value);
                if(true == request?.OrderBy?.Any())
                    entities = entities.OrderBy(request.OrderBy);
                if(true == request?.OrderByDesc?.Any())
                    entities = entities.OrderByDescending(request.OrderByDesc);
            });
            return entities;
        }

        public List<MeanRangeValue> Post(MeanRangeValueSearch request) => Get(request);

        public List<MeanRangeValue> Get(MeanRangeValueSearch request) => GetSearchResult<MeanRangeValue,DocEntityMeanRangeValue,MeanRangeValueSearch>(DocConstantModelName.MEANRANGEVALUE, request, _ExecSearch);

        public MeanRangeValue Get(MeanRangeValue request) => GetEntity<MeanRangeValue>(DocConstantModelName.MEANRANGEVALUE, request, GetMeanRangeValue);
        private MeanRangeValue _AssignValues(MeanRangeValue request, DocConstantPermission permission, Session session)
        {
            if(permission != DocConstantPermission.ADD && (request == null || request.Id <= 0))
                throw new HttpError(HttpStatusCode.NotFound, $"No record");

            if(permission == DocConstantPermission.ADD && !DocPermissionFactory.HasPermissionTryAdd(currentUser, "MeanRangeValue"))
                throw new HttpError(HttpStatusCode.Forbidden, "You do not have ADD permission for this route.");

            request.VisibleFields = request.VisibleFields ?? new List<string>();

            MeanRangeValue ret = null;
            request = _InitAssignValues<MeanRangeValue>(request, permission, session);
            //In case init assign handles create for us, return it
            if(permission == DocConstantPermission.ADD && request.Id > 0) return request;
            
            var cacheKey = GetApiCacheKey<MeanRangeValue>(DocConstantModelName.MEANRANGEVALUE, nameof(MeanRangeValue), request);
            
            //First, assign all the variables, do database lookups and conversions
            DocEntityLookupTable pMeanVarianceType = GetLookup(DocConstantLookupTable.MEANVARIANCETYPE, request.MeanVarianceType?.Name, request.MeanVarianceType?.Id);
            var pMidSpread = request.MidSpread;
            var pOrder = request.Order;
            var pOwners = request.Owners?.ToList();
            var pPercent = request.Percent;
            var pPercentLow = request.PercentLow;
            var pRange = request.Range;
            DocEntityLookupTable pType = GetLookup(DocConstantLookupTable.MEANRANGETYPE, request.Type?.Name, request.Type?.Id);

            DocEntityMeanRangeValue entity = null;
            if(permission == DocConstantPermission.ADD)
            {
                var now = DateTime.UtcNow;
                entity = new DocEntityMeanRangeValue(session)
                {
                    Created = now,
                    Updated = now
                };
            }
            else
            {
                entity = DocEntityMeanRangeValue.GetMeanRangeValue(request.Id);
                if(null == entity)
                    throw new HttpError(HttpStatusCode.NotFound, $"No record");
            }

            if (DocPermissionFactory.IsRequestedHasPermission<DocEntityLookupTable>(currentUser, request, pMeanVarianceType, permission, DocConstantModelName.MEANRANGEVALUE, nameof(request.MeanVarianceType)))
            {
                if(DocPermissionFactory.IsRequested(request, pMeanVarianceType, entity.MeanVarianceType, nameof(request.MeanVarianceType)))
                    entity.MeanVarianceType = pMeanVarianceType;
                if(DocPermissionFactory.IsRequested<DocEntityLookupTable>(request, pMeanVarianceType, nameof(request.MeanVarianceType)) && !request.VisibleFields.Matches(nameof(request.MeanVarianceType), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.MeanVarianceType));
                }
            }
            if (DocPermissionFactory.IsRequestedHasPermission<TypeUnits>(currentUser, request, pMidSpread, permission, DocConstantModelName.MEANRANGEVALUE, nameof(request.MidSpread)))
            {
                if(DocPermissionFactory.IsRequested(request, pMidSpread, entity.MidSpread, nameof(request.MidSpread)))
                    entity.MidSpread = pMidSpread;
                if(DocPermissionFactory.IsRequested<TypeUnits>(request, pMidSpread, nameof(request.MidSpread)) && !request.VisibleFields.Matches(nameof(request.MidSpread), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.MidSpread));
                }
            }
            if (DocPermissionFactory.IsRequestedHasPermission<int?>(currentUser, request, pOrder, permission, DocConstantModelName.MEANRANGEVALUE, nameof(request.Order)))
            {
                if(DocPermissionFactory.IsRequested(request, pOrder, entity.Order, nameof(request.Order)))
                    if(null != pOrder)
                        entity.Order = (int) pOrder;
                if(DocPermissionFactory.IsRequested<int?>(request, pOrder, nameof(request.Order)) && !request.VisibleFields.Matches(nameof(request.Order), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.Order));
                }
            }
            if (DocPermissionFactory.IsRequestedHasPermission<decimal?>(currentUser, request, pPercent, permission, DocConstantModelName.MEANRANGEVALUE, nameof(request.Percent)))
            {
                if(DocPermissionFactory.IsRequested(request, pPercent, entity.Percent, nameof(request.Percent)))
                    entity.Percent = pPercent;
                if(DocPermissionFactory.IsRequested<decimal?>(request, pPercent, nameof(request.Percent)) && !request.VisibleFields.Matches(nameof(request.Percent), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.Percent));
                }
            }
            if (DocPermissionFactory.IsRequestedHasPermission<decimal?>(currentUser, request, pPercentLow, permission, DocConstantModelName.MEANRANGEVALUE, nameof(request.PercentLow)))
            {
                if(DocPermissionFactory.IsRequested(request, pPercentLow, entity.PercentLow, nameof(request.PercentLow)))
                    entity.PercentLow = pPercentLow;
                if(DocPermissionFactory.IsRequested<decimal?>(request, pPercentLow, nameof(request.PercentLow)) && !request.VisibleFields.Matches(nameof(request.PercentLow), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.PercentLow));
                }
            }
            if (DocPermissionFactory.IsRequestedHasPermission<TypeUnitsRange>(currentUser, request, pRange, permission, DocConstantModelName.MEANRANGEVALUE, nameof(request.Range)))
            {
                if(DocPermissionFactory.IsRequested(request, pRange, entity.Range, nameof(request.Range)))
                    entity.Range = pRange;
                if(DocPermissionFactory.IsRequested<TypeUnitsRange>(request, pRange, nameof(request.Range)) && !request.VisibleFields.Matches(nameof(request.Range), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.Range));
                }
            }
            if (DocPermissionFactory.IsRequestedHasPermission<DocEntityLookupTable>(currentUser, request, pType, permission, DocConstantModelName.MEANRANGEVALUE, nameof(request.Type)))
            {
                if(DocPermissionFactory.IsRequested(request, pType, entity.Type, nameof(request.Type)))
                    entity.Type = pType;
                if(DocPermissionFactory.IsRequested<DocEntityLookupTable>(request, pType, nameof(request.Type)) && !request.VisibleFields.Matches(nameof(request.Type), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.Type));
                }
            }
            
            if (request.Locked) entity.Locked = request.Locked;

            entity.SaveChanges(permission);
            
            if (DocPermissionFactory.IsRequestedHasPermission<List<Reference>>(currentUser, request, pOwners, permission, DocConstantModelName.MEANRANGEVALUE, nameof(request.Owners)))
            {
                if (true == pOwners?.Any() )
                {
                    var requestedOwners = pOwners.Select(p => p.Id).Distinct().ToList();
                    var existsOwners = Execute.SelectAll<DocEntityMeanRanges>().Where(e => e.Id.In(requestedOwners)).Select( e => e.Id ).ToList();
                    if (existsOwners.Count != requestedOwners.Count)
                    {
                        var nonExists = requestedOwners.Where(id => existsOwners.All(eId => eId != id));
                        throw new HttpError(HttpStatusCode.NotFound, $"Cannot patch collection Owners with objects that do not exist. No matching Owners(s) could be found for Ids: {nonExists.ToDelimitedString()}.");
                    }
                    var toAdd = requestedOwners.Where(id => entity.Owners.All(e => e.Id != id)).ToList(); 
                    toAdd?.ForEach(id =>
                    {
                        var target = DocEntityMeanRanges.GetMeanRanges(id);
                        if(!DocPermissionFactory.HasPermission(entity, currentUser, DocConstantPermission.ADD, targetEntity: target, targetName: nameof(MeanRangeValue), columnName: nameof(request.Owners)))
                            throw new HttpError(HttpStatusCode.Forbidden, "You do not have permission to add {nameof(request.Owners)} to {nameof(MeanRangeValue)}");
                        entity.Owners.Add(target);
                    });
                    var toRemove = entity.Owners.Where(e => requestedOwners.All(id => e.Id != id)).Select(e => e.Id).ToList(); 
                    toRemove.ForEach(id =>
                    {
                        var target = DocEntityMeanRanges.GetMeanRanges(id);
                        if(!DocPermissionFactory.HasPermission(entity, currentUser, DocConstantPermission.REMOVE, targetEntity: target, targetName: nameof(MeanRangeValue), columnName: nameof(request.Owners)))
                            throw new HttpError(HttpStatusCode.Forbidden, "You do not have permission to remove {nameof(request.Owners)} from {nameof(MeanRangeValue)}");
                        entity.Owners.Remove(target);
                    });
                }
                else
                {
                    var toRemove = entity.Owners.Select(e => e.Id).ToList(); 
                    toRemove.ForEach(id =>
                    {
                        var target = DocEntityMeanRanges.GetMeanRanges(id);
                        if(!DocPermissionFactory.HasPermission(entity, currentUser, DocConstantPermission.REMOVE, targetEntity: target, targetName: nameof(MeanRangeValue), columnName: nameof(request.Owners)))
                            throw new HttpError(HttpStatusCode.Forbidden, "You do not have permission to remove {nameof(request.Owners)} from {nameof(MeanRangeValue)}");
                        entity.Owners.Remove(target);
                    });
                }
                if(DocPermissionFactory.IsRequested<List<Reference>>(request, pOwners, nameof(request.Owners)) && !request.VisibleFields.Matches(nameof(request.Owners), ignoreSpaces: true))
                {
                    request.VisibleFields.Add(nameof(request.Owners));
                }
            }
            DocPermissionFactory.SetVisibleFields<MeanRangeValue>(currentUser, nameof(MeanRangeValue), request.VisibleFields);
            ret = entity.ToDto();

            DocCacheClient.Set(key: cacheKey, value: ret, entityId: request.Id, entityType: DocConstantModelName.MEANRANGEVALUE);

            return ret;
        }
        public MeanRangeValue Post(MeanRangeValue request)
        {
            if(request == null) throw new HttpError(HttpStatusCode.NotFound, "Request cannot be null.");

            request.VisibleFields = request.VisibleFields ?? new List<string>();

            MeanRangeValue ret = null;

            Execute.Run(ssn =>
            {
                if(!DocPermissionFactory.HasPermissionTryAdd(currentUser, "MeanRangeValue")) 
                    throw new HttpError(HttpStatusCode.Forbidden, "You do not have ADD permission for this route.");

                ret = _AssignValues(request, DocConstantPermission.ADD, ssn);
            });

            return ret;
        }
   
        public List<MeanRangeValue> Post(MeanRangeValueBatch request)
        {
            if(true != request?.Any()) throw new HttpError(HttpStatusCode.NotFound, "Request cannot be empty.");

            var ret = new List<MeanRangeValue>();
            var errors = new List<ResponseError>();
            var errorMap = new Dictionary<string, string>();
            var i = 0;
            request.ForEach(dto =>
            {
                try
                {
                    var obj = Post(dto) as MeanRangeValue;
                    ret.Add(obj);
                    errorMap[$"{i}"] = $"{obj.Id}";
                }
                catch (Exception ex)
                {
                    errorMap[$"{i}"] = null;
                    errors.Add(new ResponseError()
                    {
                        Message = $"{ex.Message}{Environment.NewLine}{ex.InnerException?.Message}",
                        ErrorCode = $"{i}"
                    });
                }
                i += 1;
            });
            base.Response.AddHeader("X-AutoBatch-Completed", $"{ret.Count} succeeded");
            if (errors.Any())
            {
                throw new HttpError(HttpStatusCode.BadRequest, $"{errors.Count} failed in batch")
                {
                    Response = new ErrorResponse()
                    {
                        ResponseStatus = new ResponseStatus
                        {
                            ErrorCode = nameof(HttpError),
                            Meta = errorMap,
                            Message = "Incomplete request",
                            Errors = errors
                        }
                    }
                };
            }
            return ret;
        }

        public MeanRangeValue Post(MeanRangeValueCopy request)
        {
            MeanRangeValue ret = null;
            Execute.Run(ssn =>
            {
                var entity = DocEntityMeanRangeValue.GetMeanRangeValue(request?.Id);
                if(null == entity) throw new HttpError(HttpStatusCode.NoContent, "The COPY request did not succeed.");
                if(!DocPermissionFactory.HasPermission(entity, currentUser, DocConstantPermission.ADD))
                    throw new HttpError(HttpStatusCode.Forbidden, "You do not have ADD permission for this route.");
                
                    var pMeanVarianceType = entity.MeanVarianceType;
                    var pMidSpread = entity.MidSpread;
                    var pOrder = entity.Order;
                    var pOwners = entity.Owners.ToList();
                    var pPercent = entity.Percent;
                    var pPercentLow = entity.PercentLow;
                    var pRange = entity.Range;
                    var pType = entity.Type;
                #region Custom Before copyMeanRangeValue
                #endregion Custom Before copyMeanRangeValue
                var copy = new DocEntityMeanRangeValue(ssn)
                {
                    Hash = Guid.NewGuid()
                                , MeanVarianceType = pMeanVarianceType
                                , MidSpread = pMidSpread
                                , Order = pOrder
                                , Percent = pPercent
                                , PercentLow = pPercentLow
                                , Range = pRange
                                , Type = pType
                };
                            foreach(var item in pOwners)
                            {
                                entity.Owners.Add(item);
                            }

                #region Custom After copyMeanRangeValue
                #endregion Custom After copyMeanRangeValue
                copy.SaveChanges(DocConstantPermission.ADD);
                ret = copy.ToDto();
            });
            return ret;
        }


        public List<MeanRangeValue> Put(MeanRangeValueBatch request)
        {
            return Patch(request);
        }

        public MeanRangeValue Put(MeanRangeValue request)
        {
            return Patch(request);
        }

        public List<MeanRangeValue> Patch(MeanRangeValueBatch request)
        {
            if(true != request?.Any()) throw new HttpError(HttpStatusCode.NotFound, "Request cannot be empty.");

            var ret = new List<MeanRangeValue>();
            var errors = new List<ResponseError>();
            var errorMap = new Dictionary<string, string>();
            var i = 0;
            request.ForEach(dto =>
            {
                try
                {
                    var obj = Patch(dto) as MeanRangeValue;
                    ret.Add(obj);
                    errorMap[$"{i}"] = $"true";
                }
                catch (Exception ex)
                {
                    errorMap[$"{i}"] = $"false";
                    errors.Add(new ResponseError()
                    {
                        Message = $"{ex.Message}{Environment.NewLine}{ex.InnerException?.Message}",
                        ErrorCode = $"{i}"
                    });
                }
                i += 1;
            });
            base.Response.AddHeader("X-AutoBatch-Completed", $"{ret.Count} succeeded");
            if (errors.Any())
            {
                throw new HttpError(HttpStatusCode.BadRequest, $"{errors.Count} failed in batch")
                {
                    Response = new ErrorResponse()
                    {
                        ResponseStatus = new ResponseStatus
                        {
                            ErrorCode = nameof(HttpError),
                            Meta = errorMap,
                            Message = "Incomplete request",
                            Errors = errors
                        }
                    }
                };
            }
            return ret;
        }

        public MeanRangeValue Patch(MeanRangeValue request)
        {
            if(true != (request?.Id > 0)) throw new HttpError(HttpStatusCode.NotFound, "Please specify a valid Id of the MeanRangeValue to patch.");
            
            request.VisibleFields = request.VisibleFields ?? new List<string>();
            
            MeanRangeValue ret = null;
            Execute.Run(ssn =>
            {
                ret = _AssignValues(request, DocConstantPermission.EDIT, ssn);
            });
            return ret;
        }

        public void Delete(MeanRangeValueBatch request)
        {
            if(true != request?.Any()) throw new HttpError(HttpStatusCode.NotFound, "Request cannot be empty.");

            var errors = new List<ResponseError>();
            var errorMap = new Dictionary<string, string>();
            var i = 0;
            request.ForEach(dto =>
            {
                try
                {
                    Delete(dto);
                    errorMap[$"{i}"] = $"true";
                }
                catch (Exception ex)
                {
                    errorMap[$"{i}"] = $"false";
                    errors.Add(new ResponseError()
                    {
                        Message = $"{ex.Message}{Environment.NewLine}{ex.InnerException?.Message}",
                        ErrorCode = $"{i}"
                    });
                }
                i += 1;
            });
            base.Response.AddHeader("X-AutoBatch-Completed", $"{request.Count-errors.Count} succeeded");
            if (errors.Any())
            {
                throw new HttpError(HttpStatusCode.BadRequest, $"{errors.Count} failed in batch")
                {
                    Response = new ErrorResponse()
                    {
                        ResponseStatus = new ResponseStatus
                        {
                            ErrorCode = nameof(HttpError),
                            Meta = errorMap,
                            Message = "Incomplete request",
                            Errors = errors
                        }
                    }
                };
            }
        }

        public void Delete(MeanRangeValue request)
        {
            Execute.Run(ssn =>
            {
                if(!(request?.Id > 0)) throw new HttpError(HttpStatusCode.NotFound, $"No Id provided for delete.");

                DocCacheClient.RemoveSearch(DocConstantModelName.MEANRANGEVALUE);
                DocCacheClient.RemoveById(request.Id);
                var en = DocEntityMeanRangeValue.GetMeanRangeValue(request?.Id);

                if(null == en) throw new HttpError(HttpStatusCode.NotFound, $"No MeanRangeValue could be found for Id {request?.Id}.");
                if(en.IsRemoved) return;
                
                if(!DocPermissionFactory.HasPermission(en, currentUser, DocConstantPermission.DELETE))
                    throw new HttpError(HttpStatusCode.Forbidden, "You do not have DELETE permission for this route.");
                
                en.Remove();
            });
        }

        public void Delete(MeanRangeValueSearch request)
        {
            var matches = Get(request) as List<MeanRangeValue>;
            if(true != matches?.Any()) throw new HttpError(HttpStatusCode.NotFound, "No matches for request");

            Execute.Run(ssn =>
            {
                matches.ForEach(match =>
                {
                    Delete(match);
                });
            });
        }
        public object Get(MeanRangeValueJunction request) =>
            Execute.Run( s => 
            {
                switch(request.Junction.ToLower().TrimAndPruneSpaces())
                {
                    case "meanranges":
                        return GetJunctionSearchResult<MeanRangeValue, DocEntityMeanRangeValue, DocEntityMeanRanges, MeanRanges, MeanRangesSearch>((int)request.Id, DocConstantModelName.MEANRANGES, "Owners", request, (ss) => HostContext.ResolveService<MeanRangesService>(Request)?.Get(ss));
                    default:
                        throw new HttpError(HttpStatusCode.NotFound, $"Route for meanrangevalue/{request.Id}/{request.Junction} was not found");
                }
            });
        public object Post(MeanRangeValueJunction request) =>
            Execute.Run( ssn =>
            {
                switch(request.Junction.ToLower().TrimAndPruneSpaces())
                {
                    case "meanranges":
                        return AddJunction<MeanRangeValue, DocEntityMeanRangeValue, DocEntityMeanRanges, MeanRanges, MeanRangesSearch>((int)request.Id, DocConstantModelName.MEANRANGES, "Owners", request);
                    default:
                        throw new HttpError(HttpStatusCode.NotFound, $"Route for meanrangevalue/{request.Id}/{request.Junction} was not found");
                }
            });

        public object Delete(MeanRangeValueJunction request) =>
            Execute.Run( ssn =>
            {
                switch(request.Junction.ToLower().TrimAndPruneSpaces())
                {
                    case "meanranges":
                        return RemoveJunction<MeanRangeValue, DocEntityMeanRangeValue, DocEntityMeanRanges, MeanRanges, MeanRangesSearch>((int)request.Id, DocConstantModelName.MEANRANGES, "Owners", request);
                    default:
                        throw new HttpError(HttpStatusCode.NotFound, $"Route for meanrangevalue/{request.Id}/{request.Junction} was not found");
                }
            });

        private MeanRangeValue GetMeanRangeValue(MeanRangeValue request)
        {
            var id = request?.Id;
            MeanRangeValue ret = null;
            var query = DocQuery.ActiveQuery ?? Execute;

            DocPermissionFactory.SetVisibleFields<MeanRangeValue>(currentUser, "MeanRangeValue", request.VisibleFields);

            DocEntityMeanRangeValue entity = null;
            if(id.HasValue)
            {
                entity = DocEntityMeanRangeValue.GetMeanRangeValue(id.Value);
            }
            if(null == entity)
                throw new HttpError(HttpStatusCode.NotFound, $"No MeanRangeValue found for Id {id.Value}");

            if(!DocPermissionFactory.HasPermission(entity, currentUser, DocConstantPermission.VIEW))
                throw new HttpError(HttpStatusCode.Forbidden, "You do not have VIEW permission for this route.");
            
            ret = entity?.ToDto();
            return ret;
        }
    }
}