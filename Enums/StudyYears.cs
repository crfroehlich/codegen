
//------------------------------------------------------------------------------
// <auto-generated>
//    This code is auto-generated by a T4 template. This class can be only customized modifying the corresponding partial class.
//    All other changes to this file will cause incorrect behavior and will be lost when the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Linq;
using System.Linq.Dynamic;
using System.Reflection;
using System.Runtime.Serialization;

using Newtonsoft.Json;
using Newtonsoft.Json.Converters;

using Services.Core;

using ServiceStack;

using SCDescript = System.ComponentModel.DescriptionAttribute;
using SCDisplay = System.ComponentModel.DataAnnotations.DisplayAttribute;
using SSDescript = ServiceStack.DataAnnotations.DescriptionAttribute;
namespace Services.Enums
{
    [JsonConverter(typeof(StringEnumConverter))]
    public enum StudyYearsEnm
    {
        [EnumMember(Value = DocConstantStudyYears.ANALYZED), SCDescript(DocConstantStudyYears.ANALYZED), SSDescript(DocConstantStudyYears.ANALYZED), SCDisplay(Name = DocConstantStudyYears.ANALYZED)]
        ANALYZED = 3773,
        [EnumMember(Value = DocConstantStudyYears.CONDUCTED), SCDescript(DocConstantStudyYears.CONDUCTED), SSDescript(DocConstantStudyYears.CONDUCTED), SCDisplay(Name = DocConstantStudyYears.CONDUCTED)]
        CONDUCTED = 3778,
        [EnumMember(Value = DocConstantStudyYears.ENROLLMENT), SCDescript(DocConstantStudyYears.ENROLLMENT), SSDescript(DocConstantStudyYears.ENROLLMENT), SCDisplay(Name = DocConstantStudyYears.ENROLLMENT)]
        ENROLLMENT = 3783,
        [EnumMember(Value = DocConstantStudyYears.RECRUITMENT), SCDescript(DocConstantStudyYears.RECRUITMENT), SSDescript(DocConstantStudyYears.RECRUITMENT), SCDisplay(Name = DocConstantStudyYears.RECRUITMENT)]
        RECRUITMENT = 3788
    }
    
    public static partial class EnumExtensions
    {
        public static string ToEnumString(this StudyYearsEnm instance)
        {
            switch(instance)
            {
                case StudyYearsEnm.ANALYZED:
                    return DocConstantStudyYears.ANALYZED;
                case StudyYearsEnm.CONDUCTED:
                    return DocConstantStudyYears.CONDUCTED;
                case StudyYearsEnm.ENROLLMENT:
                    return DocConstantStudyYears.ENROLLMENT;
                case StudyYearsEnm.RECRUITMENT:
                    return DocConstantStudyYears.RECRUITMENT;
                default:
                    return string.Empty;
            }
        }

        public static string ToEnumString(this StudyYearsEnm? instance)
        {
            if(null == instance) return string.Empty;
            return instance.Value.ToEnumString();
        }
    }

    public sealed partial class DocConstantStudyYears : IEquatable<DocConstantStudyYears>, IEqualityComparer<DocConstantStudyYears>
    {
        public const string ANALYZED = "Analyzed";
        public const string CONDUCTED = "Conducted";
        public const string ENROLLMENT = "Enrollment";
        public const string RECRUITMENT = "Recruitment";
        
        #region Internals
        
        private static List<string> _all;
        public static List<string> All => _all ?? (_all = typeof(DocConstantStudyYears).GetFields(BindingFlags.Public | BindingFlags.Static | BindingFlags.FlattenHierarchy).Where(fi => fi.IsLiteral && !fi.IsInitOnly).Select( fi => fi.GetRawConstantValue().ToString() ).OrderBy(n => n).ToList());

        private readonly string Value;

        private DocConstantStudyYears(string ItemName = null)
        {
            ItemName = ItemName ?? string.Empty;
            Value = FirstOrDefault(ItemName) ?? ItemName;
        }

        public static bool Contains(string name) => All.Any(val => string.Equals(val, name, StringComparison.OrdinalIgnoreCase));
        
        public static string FirstOrDefault(string name) => All.FirstOrDefault(val => string.Equals(val, name, StringComparison.OrdinalIgnoreCase));

        public static implicit operator DocConstantStudyYears(string Val) => new DocConstantStudyYears(Val);

        public static implicit operator string(DocConstantStudyYears item) => item?.Value ?? string.Empty;
        
        public static explicit operator DocConstantStudyYears(StudyYearsEnm Val) => new DocConstantStudyYears(Val.ToEnumString());

        public static explicit operator StudyYearsEnm(DocConstantStudyYears item)
        {
            Enum.TryParse<StudyYearsEnm>(item.ToString(), true, out var tryRet);
            return tryRet;
        }

        public override string ToString() => Value;

        #endregion Internals

        #region IEquatable

        public bool Equals(DocConstantStudyYears obj) => this == obj;

        public static bool operator ==(DocConstantStudyYears x, DocConstantStudyYears y) => DocTools.AreEqual(DocConvert.ToString(x), DocConvert.ToString(y));
        
        public bool Equals(DocConstantStudyYears x, DocConstantStudyYears y) => x == y;
        
        public static bool operator !=(DocConstantStudyYears x, DocConstantStudyYears y) => !(x == y);

        public override bool Equals(object obj)
        {
            var ret = false;
            if(!(obj is DocConstantStudyYears))
            {
                ret = false;
            }
            else
            {
                ret = this == (DocConstantStudyYears) obj;
            }
            return ret;
        }

        public override int GetHashCode() => 17 * Value?.GetHashCode() ?? -1;
                
        public int GetHashCode(DocConstantStudyYears obj) => obj?.GetHashCode() ?? -17;

        #endregion IEquatable
    }
}
